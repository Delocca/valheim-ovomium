# Journal de session

Une entrée par jour de travail, plus récent en haut : fait, décisions (avec leurs raisons), en suspens. Ce que git dit déjà n'y va pas. Règles : `CLAUDE.md`, section « Suivi ».

## 2026-09-19 — Boucle de test rapide, rafale de features, suivi

- **Fait** : déploiement atomique jeu lancé + relance auto (`deploy.sh --relaunch`), AutoJoin, hot reload ScriptEngine avec `Unload()` par feature, autocontrôle des patches au chargement, puis FocusClick, ContinueButton, MapExplore, PortalRange, ButcherKnife, AmbientOcclusion, UpgradeDiff, SkillTooltip, TooltipStyle, SettingsMenu en direct. Tout validé en jeu par Edia. Mise en place de `TODO.md`, `CHANGELOG.md` et de ce journal ; `release.sh` prend ses notes dans le changelog. Updater (TODO 5) livré et validé en jeu par un vrai cycle (DLL de test en 0.7.0, dépôt ouvert, release 0.8.0 détectée, fenêtre avec changelog, téléchargement, patcher « Ovomium 0.8.0 installée » à la relance ; cas AutoJoin : message HUD puis fenêtre au menu Échap) : solution à deux projets (`Ovomium.sln`, `Directory.Build.props`), patcher `Ovomium.Updater` ajouté au zip et à `deploy.sh`.
- **Décisions** : Edia teste sur son serveur pendant que je code et ferme le jeu elle-même (jamais moi). Pipette de construction abandonnée : vanilla (Maj + clic molette, `Player.CopyPiece`). Graphismes vanilla en direct reportés à une session neuve. Apparition au login laissée vanilla (2026-09-17). ContinueButton en texte plutôt qu'icônes du jeu (pas assez parlantes). Updater : patcher BepInEx plutôt que mini plugin à dépendance (s'exécute avant tout chargement de plugin par construction, sans ordre à déclarer) ; changelog = `body` de la release (déjà les notes du changelog via `release.sh`), affiché dans le `YesNoPopup` vanilla ; cas AutoJoin (menu sauté) : message HUD puis popup au premier menu Échap ; le patcher et BepInEx restent du ressort de l'installateur `.bat` (un patcher chargé ne peut pas se remplacer) ; seul le dossier plugin de l'archive est installé. Leçon du premier essai : la fenêtre s'affichait pendant la jonction AutoJoin (l'écran `Loading` n'est actif qu'au chargement effectif, pas pendant la résolution PlayFab), puis disparaissait avec la scène ; désormais fenêtre seulement sur un écran de menu visible, et un drapeau « affichée sans réponse » remis à zéro si elle a disparu, pour la reproposer au menu Échap. Une fenêtre vanilla empilée dessous (« Connexion impossible ») réapparaît à la fermeture de la nôtre : comportement normal de la pile `UnifiedPopup`.
- **Fait (suite)** : CraftFromChests, après repérage des mods existants (CraftFromContainers d'aedenthorn : domaine public, à jour 1.0, ancrages validés ; AzuCraftyBoxes : abandonné, sans contrôle d'accès ; McCraftReach et RunicCrafting : seuls à décrire un protocole de possession du ZDO correct) et du décompilé. Ancrages : `Player.HaveRequirementItems` et `HaveRequirements(Piece, mode)` lisent `m_inventory` en direct (postfix qui refait le calcul vanilla avec les coffres), `Player.ConsumeResources` point unique de consommation (craft, amélioration, marteau), `InventoryGui.SetupRequirement` statique partagée craft + HUD marteau. Piège 1.0 reproduit : 271 recettes sur 481 portent un ingrédient `m_upgraderResource`, à ignorer sauf sur une station `m_upgrader`.
- **Décisions** : rayon autour du joueur, réglable (plus prévisible que la portée de la station, et le marteau n'a pas de station), porté de 10 à 50 m par défaut après les tests, plafond 150 m : au-delà les coffres ne sont plus instanciés (zones de 64 m, `Piece.s_allPieces` ne voit que le chargé) ; affichage « requis (total) » (petit et vert, après un passage par gris puis blanc lors des tests d'Edia) avec le rouge clignotant seulement si le total manque (vanilla n'affiche pas le possédé) ; chariots et bateaux inclus (réserve garée près de la forge). Multijoueur : prise de possession unilatérale du ZDO + `ForceSendZDO` à l'ancien propriétaire (moitié « réponse » de « Tout prendre »), coffres marqués ouverts dans le ZDO ignorés ; la demande au propriétaire (RPC asynchrone) ne s'insère pas dans `ConsumeResources`, synchrone. Edia a ensuite choisi (option « faire le protocole tout de suite ») la réservation anticipée `ChestReservation` : entre amies actives sur la même réserve, la fenêtre d'une seconde donne de vrais doublons ou pertes. Panneau de craft ouvert ou marteau en main, toutes les 2 s, `RPC_RequestOpen` vanilla à la propriétaire de chaque coffre à portée ; sa réponse (refus si ouvert chez elle, sinon `ForceSendZDO` + `SetOwner`) est avalée par un prefix de `RPC_OpenResponse` ; un vrai clic (`Container.Interact`) retire le coffre de l'attente pour que la fenêtre s'ouvre ; sans réponse en 1 s, repli sur la prise directe (tracée dans le journal). Vérifié dans le décompilé : `ZNetView.InvokeRPC(method)` cible le propriétaire du ZDO ; propriétaire 0 = diffusion à tous (d'où `ClaimOwnership` direct dans ce cas) ; sur serveur dédié, `ZDOMan` attribue les ZDO sans propriétaire au pair dont la zone est active, donc c'est le jeu de l'amie qui répond.
- **Validé en solo** : recettes de chaudron et mur au marteau, retraits tracés cohérents (`LogPulls`), coffres débités par le bas à droite. Edia a ensuite inversé l'ordre : coffres d'abord, inventaire en dernier (option `ChestsFirst`, oui par défaut) : ce qu'on porte reste disponible loin des coffres.
- **Fait (soir)** : ItemFlight, l'animation des ingrédients volant des coffres, réglée en une dizaine d'allers-retours à chaud avec Edia. Ancrages : départ dès `InventoryGui.OnCraftPressed` (`m_craftTimer` passé à 0) sur une prévision des retraits (`NearbyChests.Plan`, extrait de `Remove` qui l'exécute désormais : une seule logique de choix), confirmation par l'événement `Consumed` du prefix de `ConsumeResources`, annulation quand `m_craftTimer` repasse à -1 sans consommation (postfix de `InventoryGui.Update`) ; marteau : cible relevée sur le fantôme dans un prefix de `PlacePiece`, même frame que la consommation. Objet : enfant `attach` du prefab, sinon prefab entier hors réseau (`ZNetView.m_forceDisableInit`) réduit à ses rendus par `DestroyImmediate` (scripts, puis joints, colliders, Rigidbody : sinon `Player.AutoPickup` déréférence le ZNetView détruit). Traînée : pas de particules construites par code dans le jeu et aucun nom de prefab VFX dans le décompilé ; on clone les enfants à particules du projectile de la flèche de feu (`m_shared.m_attack.m_attackProjectile` via `ObjectDB`), quatre systèmes (`trail` étincelles blanches, `flames`, `flames_local`, `flare` halo de 2 m) dont on ne garde que `flames`, converti en sillage émis par mètre, immobile, vie 10 s avec fondu.
- **Décisions d'Edia (ItemFlight)** : départ au début du craft avec prédiction plutôt qu'à la consommation ; local seulement ; prefab 3D plutôt qu'icône ; destination = centre des rendus (un chaudron pendu à son trépied : le sommet des bornes était le trépied) ; départ = face avant du coffre à 60 % de sa hauteur ; pas d'arc global : ligne quasi droite ondulée (faible amplitude, 3 oscillations) qui s'incurve pour descendre 50 cm dans la cible, une seule Bézier plutôt qu'un plongeon séparé (coupure visible) ; vitesse constante le long de la courbe (table de longueurs d'arc : la vitesse paramétrique d'une Bézier s'effondre sur une tangente finale courte, l'objet faisait du sur-place en tournant) avec ease-out gardant 30 % de vitesse ; racine sans `LookRotation` (à la verticale finale, elle pivotait brutalement) ; grossissement et rétrécissement de 300 ms aux deux bouts ; défauts = ses réglages en jeu. Piège : BepInEx garde la valeur enregistrée dans le cfg quand on change un défaut.
- **En suspens** : CraftFromChests sur le serveur avec une amie (réservation, coffre ouvert chez elle, craft simultané), release 0.9.0 (`TODO.md`). ItemFlight validé en jeu (chaudron, Annuler pendant la barre : tout disparaît sur-le-champ, mur au marteau).
